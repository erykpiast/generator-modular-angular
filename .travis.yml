language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-modular-angular-require' ]; then cd .. && eval "mv $currentfolder generator-modular-angular-require" && cd generator-modular-angular-require; fi

